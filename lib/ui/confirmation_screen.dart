import 'package:flutter/material.dart';

class ConnfirmationScreen extends StatelessWidget {
  const ConnfirmationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Did You take the medicine?'),
    );
  }
}